#my-flask-app
# My Flask Image Prediction App

This repository demonstrates a full-stack project using Flask as the backend to serve a machine learning model for image prediction and a frontend to capture or upload images. The project is deployed on Vercel as a serverless Python function.

## Project Structure

